import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import AlertBanner from '../../ch/components/AlertBanner.vue'
import * as AlertBannerStories from './AlertBanner.stories'

<Meta of={AlertBannerStories} />

# AlertBanner

Use this component only for very important notifications, for example when the population is facing a danger: Flooding, landslides, earthquakes, etc. For more common and flexible use cases, use the [Notification component](?path=/docs/components-notification--example).

## Example

<Canvas of={AlertBannerStories.Example} />

<Controls of={AlertBannerStories.Example} />
