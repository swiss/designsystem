import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Badge from '../../ch/components/Badge.vue';

<Meta
  title="Components/Badge"
  component={Badge}
  argTypes={{
    color: { control: { type: 'select', options: ['gray', 'red', 'yellow', 'orange', 'green', 'blue', 'indigo', 'purple', 'pink'] } },
    size: { control: { type: 'select', options: ['sm', 'xs'] } },
    remove: { name: 'remove icon', control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Badge },
  template: '<Badge :color="color" :size="size" :remove="remove" :label="label" />',
});

# Badge

## Example

<Canvas>
  <Story
    name="Example"
    args={{
      color: 'gray',
      size: 'sm',
      remove: false,
      label: 'Badge',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example" />

[Example](?path=/story/components-badge--example)

## Colors
For color variations, the following css classes are built:
- `badge--gray` for gray
- `badge--red` for red
- `badge--yellow` for yellow

<Canvas>
    <button class="badge badge--sm badge--gray">gray</button>
    <button class="badge badge--sm badge--red">red</button>
    <button class="badge badge--sm badge--yellow">yellow</button>
    <button class="badge badge--sm badge--orange">orange</button>
    <button class="badge badge--sm badge--green">green</button>
    <button class="badge badge--sm badge--blue">blue</button>
    <button class="badge badge--sm badge--indigo">indigo</button>
    <button class="badge badge--sm badge--purple">purple</button>
    <button class="badge badge--sm badge--pink">pink</button>
</Canvas>


## Sizes
For size variations, the following css classes are built:
- `badge--sm` for sm
- `badge--xs` for xs

<Canvas>
    <button class="badge badge--sm badge--gray">sm</button>
    <button class="badge badge--xs badge--gray">xs</button>
</Canvas>

## Icons
For icon variations, the following css classes are built:
- `badge--icon` for icon

<Canvas>
  <div class="badge badge--gray badge--sm badge--icon">
    <span class="badge__text">icon</span>
    <svg
      viewBox="0 0 24 24"
      xmlns="http://www.w3.org/2000/svg"
      class="badge__icon icon icon--base"
    >
      <path
        xmlns="http://www.w3.org/2000/svg"
        d="m17.153 7.066-4.8 4.801-4.779-4.779-.53.531 4.779 4.778-4.757 4.756.531.531 4.756-4.757 4.778 4.779.531-.53-4.779-4.779 4.801-4.8z"
      ></path>
    </svg>
  </div>
</Canvas>
