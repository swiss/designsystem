import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Pagination from '../../ch/components/Pagination.vue';

<Meta
  title="Components/Pagination"
  component={Pagination}
  argTypes={{
    type:    { control: { type: 'select', options: ['outline', 'outline-negative'] } },
    size:    { control: { type: 'select', options: ['base', 'lg'] } },
    field:   { control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Pagination },
  template: '<Pagination :type="type" :size="size" :field="field" :currentPage="currentPage" :totalPages="totalPages" :paginationItems="paginationItems" />',
});

# Pagination

## Default Compact Pagination

<Canvas>
  <Story
    name="Example"
    args={{
      currentPage: '1',
      totalPages: 'von 13 Seiten',
      paginationItems: [
        {
          icon: "ChevronLeft",
          label: "Previous Page",
          link: "#prev"
        },
        {
          icon: "ChevronRight",
          label: "Next Page",
          link: "#next"
        }
      ]
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example" />

# Extended pagination

<Canvas>
  <Story
    name="Example2"
    args={{
      type: 'outline',
      size: 'base',
      field: false,
      paginationItems: [
        {
          icon: "ChevronLineLeft",
          label: "Back to first Page",
          link: "#1"
        },
        {
          icon: "ChevronLeft",
          label: "Previous Page",
          link: "#prev"
        },
        {
          label: "1",
          link: "#1"
        },
        {
          label: "2",
          link: "#2"
        },
        {
          label: "3",
          link: "#3"
        },
        {
          label: "...",
        },
        {
          icon: "ChevronRight",
          label: "Next Page",
          link: "#next"
        },
        {
          icon: "ChevronLineRight",
          label: "Go to last Page",
          link: "#last"
        },
      ]
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>