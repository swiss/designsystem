import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import * as PrintStories from './Print.stories'

<Meta of={PrintStories} />

# Print

<Canvas of={PrintStories.Example} />

# Interaction and accessibility

The Printbutton is a button that opens a native print dialog. It is used to print the current page.
If the CSS of the stylgeuide is loaded the print view will be optimized.

The print button should be placed within the `.share-bar` element of the page following the breadcrumb.
