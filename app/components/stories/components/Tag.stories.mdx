import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Tag from '../../ch/components/Tag.vue';

<Meta
  title="Components/Tag"
  component={Tag}
  argTypes={{
    variant:    { control: { type: 'select', options: ['default', 'active'] } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Tag },
  template: '<Tag :variant="variant" :label="label" :to="to"/>',
});

export const TemplateMultiple = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Tag },
  template: '<div><Tag v-for="item in content" :variant="item.variant" :label="item.label" :to="item.to" /></div>',
});


# Tag


## Example

Tags are similar to badges, but they are bigger and always interactive. They also ensure to have the minimal interaction-height of 44px, while having a smaller visual height.

<Canvas>
  <Story
    name="Example"
    args={{
      variant: 'default',
      label: 'Button text',
      to: 'http://www.admin.ch'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example" />


## Example as filters/tabs

Tags can also be used as an alternative to the tabs, to filter content.

<Canvas>
  <Story
    name="As filters/tabs"
    args={{
      content: [
        {
          variant: 'active',
          label: 'All',
          to: '#'
        },
        {
          variant: 'default',
          label: 'Webpages',
          to: '#'
        },
        {
          variant: 'default',
          label: 'Documents',
          to: '#'
        },
        {
          variant: 'default',
          label: 'Downloads',
          to: '#'
        }
      ]
    }}
  >
    {TemplateMultiple.bind({})}
  </Story>
</Canvas>



<a href="?path=/story/components-tag--example">
  Go to the Canvas Tab
</a>

---
<a href="?id=components-tag--example&args=&viewMode=story" target="_blank">
  Open page in full width in a new tab
</a>
