import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import Textarea from '../../ch/components/Textarea.vue'

<Meta
  title="Components/Textarea"
  component={Textarea}
  argTypes={{
    type: {
      control: {
        type: 'select',
        options: ['outline', 'negative'],
      },
    },
    size: { control: { type: 'select', options: ['sm', 'base', 'lg'] } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Textarea },
  template: '<Textarea :type="type" :size="size" />',
})

# Textarea

## Example

<Canvas>
  <Story
    name="Example"
    args={{
      type: 'outline',
      size: 'base',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example" />

## Textarea negative type

- `input--negative` for standard textareas on dark background

<Canvas style={{ backgroundColor: '#2f4356' }}>
  <textarea class="input--negative input--base" value="Input text" />
</Canvas>

## Textarea disabled state

- `input--disabled` or `disabled state on the input` to add the disabled style

<Canvas>
  <textarea class="input--outline input--base" disabled value="Input text" />
  <textarea class="input--negative input--base" disabled value="Input text" />
</Canvas>

## Sizes

For size variations, the following css classes are built:

- `input--sm` for small inputs
- `input--base` for standard size
- `input--lg` for large inputs

<Canvas>
  <textarea class="input--outline input--sm" value="input--sm" />
  <textarea class="input--outline input--base" value="input--base" />
  <textarea class="input--outline input--lg" value="input--lg" />
</Canvas>
