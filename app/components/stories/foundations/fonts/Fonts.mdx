import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import * as FontsStories from './Fonts.stories'

<Meta of={FontsStories} />

# Fonts

## Headings

For heading, the following css classes are built. These classes combine a text size and a font weight, useful for most of the titles

- `.h1` for heading 1
- `.h2` for heading 2
- `.h3` for heading 3
- `.h4` for heading 4
- `.h5` for heading 5

<Canvas of={FontsStories.Headings} />

## Text sizes

If you need to control font-size only, the following responsive css classes are built:

- `text--3xl` for 3xl
- `text--2xl` for 2xl
- `text--xl` for xl
- `text--lg` for lg
- `text--base` for base
- `text--sm` for sm
- `text--xs` for xs

<Canvas of={FontsStories.TextSize} />

## Styles and Weights

For style variations, the following css classes are built:

- `font--regular` for regular
- `font--italic` for italic
- `font--bold` for bold
- `font--bold-italic` for bold italic

<Canvas of={FontsStories.StyleVariations} />

## Super and subscripts

use `<sub>` and `<sup>` tags to create superscript and subscript text.

<Canvas of={FontsStories.Superscript} />

<Canvas of={FontsStories.Subscript} />

## Example

As example you can modify the props values in the table below to combine the different classes and see the result:

<Controls of={FontsStories.Example} />

<Canvas of={FontsStories.Example} />
