import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import * as ContainersStories from './Containers.stories'

<Meta of={ContainersStories} />

# Containers

`.container` classes and its variants give a structured horizontal constraint to the content. Depending of the viewport, it combines maximal width and horizontal paddings to define content's width in a coherent way.

`.container` is frequently a child of a `.section` element.

<Canvas
  className="storybook-demo-grid storybook-show-container"
  of={ContainersStories.ContainerExample}
/>
# Consecutive Containers

Consecutive `.container` elements adds a top padding to the second container.

The gap between two consecutive containers will be identical to the gap between two consecutive sections. Check the [Section](/docs/layouts-sections--page) documentation for more details.

<Canvas
  className="storybook-demo-grid storybook-show-container"
  of={ContainersStories.ConsecutiveContainers}
/>

# Container with flexbox

- `container--flex` displays its content horizontally

<Canvas
  className="storybook-demo-grid storybook-show-container"
  of={ContainersStories.ContainerFlexbox}
/>

# Container with 12 cols grid

`.container--grid` creates a 12 columns CSS grid. It defines the main layout within a strict but flexible structure.

## Centered sub-containers

Use the following classes for `container--grid` children:

- `container__full` fullfills the 12 columns
- `container__center--md` fills 10 columns on large viewports
- `container__center--sm` fills 8 columns on large viewports
- `container__center--xs` fills 6 columns on large viewports

Additionally, a `gap--responsive` class applies default spacings between grid items.

<Canvas
  className="storybook-demo-grid storybook-show-grid"
  of={ContainersStories.CenteredSubContainers}
/>

## Main and Aside sub-containers

- `container__main` is left aligned per default, use it in combination with `container__aside`
- `container__aside` is right aligned per default

<Canvas
  className="storybook-demo-grid storybook-show-grid"
  of={ContainersStories.MainAsideSubContainers}
/>

Use `container--grid container--reverse` on the parent to swap the columns `container__main` and `container__aside` on desktop viewports only.

<Canvas
  className="storybook-demo-grid storybook-show-grid"
  of={ContainersStories.MainAsideSubContainersReverse}
/>

use `container--grid container--reverse-mobile` on the parent to swap `container__main` and `container__aside` on mobile viewports only.
Use it when the aside content contains anchor links for example. This will display them before the main content on mobile viewports.

<Canvas
  className="storybook-demo-grid storybook-show-grid"
  of={ContainersStories.MainAsideSubContainersReverseMobile}
/>

---

# Homepage and hub pages structure

The default full width of `container` is used for hub pages and homepage. It's generallay populated with Card displayed on a grid.

```html
<main id="main-content">
  <Hero />
  <section class="section bg--accent-01">
    <div class="container">
      <h2 class="section__title">Section title</h2>
      <div class="grid grid--responsive-cols-3 gap--responsive">
        <!-- Content here, for example 3 column card list on gray/dark grey background in light/darkmode -->
      </div>
    </div>
  </section>

  <section class="section section--default">
    <div class="container">
      <h2 class="section__title">Section title</h2>
      <div class="grid grid--responsive-cols-2 gap--responsive">
        <!-- Content here, for example 2 columns card list on white background -->
      </div>
    </div>
  </section>
</main>
```

- <a href="?path=/docs/pages-homepage--example">Homepage complete example</a>
- <a href="?path=/docs/pages-hub-page--example">Hub page complete example</a>

# Default page structure

Default page's content is mainly displayed into a `container__center--xs` sub-container.

We can give a visual rythm to the page by inserting text-image blocks in a broader `container__center--sm` or `container__center--md` container, and then continue the content flow into a `container__center--xs` container again.

Consecutive sections with different full width backgrounds will give visual rythm too, for example the quote section:

```html
<main id="main-content">
  <Hero />
  <section class="section section--default">
    <div class="container container--grid gap--responsive">
      <div class="container__center--xs">
        <!-- Content flow here -->
      </div>
      <div class="container__center--sm">
        <div class="grid grid--responsive-cols-1/4-3/4 gap--responsive">
          <!-- visual break with image left and text right, the container is broader -->
        </div>
      </div>
      <div class="container__center--xs">
        <!-- Content flow continues here -->
      </div>
    </div>
  </section>

  <QuoteSection>
    <!-- visual break here, the quote takes the whole screen width -->
  </QuoteSection>

  <section class="section section--default">
    <div class="container container--grid gap--responsive">
      <div class="container__center--xs">
        <!-- Content flow continues here -->
      </div>
    </div>
  </section>
</main>
```

- <a href="?path=/docs/pages-detail-page-simple--example">
    {'Default page complete example'}
  </a>

# Complex page structure

Complex page's main content can be displayed into a `container__main` sub-container, and side content can be displayed into a `container__aside` sub-container.
Then, the content can be rythmed by consecutive sections with different backgrounds, and different rythms for text-image displays. A broader freedom should be possible while building these pages.

```html
<main id="main-content">
  <Hero />
  <section class="section section--default">
    <div class="container container--grid gap--responsive">
      <div class="container__main">
        <!-- Main content flow here -->
      </div>
      <div class="container__aside">
        <!-- Side content flow here -->
      </div>
    </div>
  </section>

  <QuoteSection>
    <!-- visual break here, the quote takes the whole screen width -->
  </QuoteSection>

  <section class="section section--default">
    <div class="container">
      <div class="grid grid--responsive-cols-2 gap--responsive">
        <!-- Content here, for example 2 columns text-image block on white background -->
      </div>
    </div>
  </section>

  <section class="section bg--accent-01">
    <div class="container">
      <div class="grid grid--responsive-cols-3 gap--responsive">
        <!-- Content here, for example 3 column card list on gray/dark grey background in light/darkmode -->
      </div>
    </div>
  </section>
</main>
```

- <a href="?path=/docs/pages-detail-page-complex--example">
    {'Complex page complete example'}
  </a>
