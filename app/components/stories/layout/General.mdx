import { Meta, Story, Canvas } from '@storybook/blocks'

<Meta title="Layouts/General" />

# Sections and containers

Schematically, a `section` contains a `container`.

- `.section` manage the vertical spacing
- `.container` manage the horizontal spacing

```html
<main id="main-content">
  <section class="section section--default">
    <div class="container">
      <!-- Content -->
    </div>
  </section>
</main>
```

Apply the `container--grid` variant to create a 12 cols grid container.

```html
<main id="main-content">
  <section class="section section--default">
    <div class="container container--grid">
      <div class="container__[variant]">
    </div>
  </section>
</main>
```

# Complete example for a simple section

On the basis explained above, additional classes adjust the global layout of a section:

```html
<main id="main-content">
  <section class="section section--default">
    <div class="container container--grid gap--responsive">
      <div class="container__center--xs">
        <!-- Content displayed in a tight and centered container -->
      </div>
      <div class="container__center--md">
        <div class="grid grid--responsive-cols-2 gap--responsive">
          <!-- Content on 2 columns, in a medium and centered container -->
        </div>
      </div>
    </div>
  </section>
</main>
```

Check the complete example of a <a href="?path=/docs/pages-detail-page-simple--example">Simple page</a>

# Example for a main + aside section

```html
<main id="main-content">
  <section class="section section--default">
    <div class="container container--grid gap--responsive">
      <div class="container__main">
        <!-- Main content, displayed in a left aligned container--grid -->
      </div>
      <div class="container__aside">
        <!-- Aside content, displayed in a right aligned container--grid -->
      </div>
    </div>
  </section>
</main>
```

Check the complete example of a <a href="?path=/docs/pages-detail-page-complex--example">Complex page</a>
