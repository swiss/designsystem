import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import * as RatiosStories from './Ratios.stories'

<Meta of={RatiosStories} />

# Ratios

`.ratio` class and its variants give a constraint to the content, especially images or video. Wrap your content with the following classes:

- `.ratio--1/1`
- `.ratio--4/3`
- `.ratio--16/9`
- `.ratio--2/1`

<Canvas className="storybook-demo-ratio" of={RatiosStories.Example} />
