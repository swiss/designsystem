
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import store from '@/.storybook/store';
import detailSimpleLanguage from '../../../pages/detailSimpleLanguage.vue';

<Meta
  title="Pages/Easy & Sign Language/Detail Easy Language"
  component={detailSimpleLanguage}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { detailSimpleLanguage },
  store: store,
  template: '<detailSimpleLanguage :isEasyLanguage="true" />',
});

# Detailpage easy language example

<a href="?path=/story/pages-easy-language-detail-easy-language--example">Go to the Canvas Tab</a>

---

<a href="?id=pages-easy-language-detail-easy-language--example" target="_blank">Open in full width in a new tab</a>

<Canvas>
  <Story
    name="Example"
  >
    {Template.bind({})}
  </Story>
</Canvas>

