
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import store from '@/.storybook/store';
import detailPublication from '../../../pages/detailPublication.vue';

<Meta
  title="Pages/Detail Publication"
  component={detailPublication}
  argTypes={{
    isOpen: { name:'Is open', control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { detailPublication },
  store: store,
  template: '<detailPublication />',
});

# Simple page example

<a href="?path=/story/pages-detail-page-simple--example">Go to the Canvas Tab</a>

---

<a href="?id=pages-detail-page-simple--example" target="_blank">Open page in full width in a new tab</a>

<Canvas>
  <Story
    name="Example"
    args={{
      isOpen: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>


