import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import store from '@/.storybook/store'
import detailPublicationCatalog from '../../../pages/detailPublicationCatalog.vue'

<Meta
  title="Pages/Publication/Katalog"
  component={detailPublicationCatalog}
  argTypes={{
    isOpen: { name: 'Is open', control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { detailPublicationCatalog },
  store: store,
  template: '<detailPublicationCatalog />',
})

# Publication page example

<a href="?path=/story/pages-publication-katalog--example">
  Go to the Canvas Tab
</a>

---

<a href="?id=pages-publication-katalog--example" target="_blank">
  Open page in full width in a new tab
</a>

<Canvas>
  <Story
    name="Example"
    args={{
      isOpen: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
