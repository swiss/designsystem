import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import store from '@/.storybook/store'
import detailPublicationShop from '../../../pages/detailPublicationShop.vue'

<Meta
  title="Pages/Publication/Shop"
  component={detailPublicationShop}
  argTypes={{
    isOpen: { name: 'Is open', control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { detailPublicationShop },
  store: store,
  template: '<detailPublicationShop />',
})

# Publication page example

<a href="?path=/story/pages-publication-shop--example">Go to the Canvas Tab</a>

---

<a href="?id=pages-publication-shop--example" target="_blank">
  Open page in full width in a new tab
</a>

<Canvas>
  <Story
    name="Example"
    args={{
      isOpen: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
