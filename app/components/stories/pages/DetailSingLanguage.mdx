import { Meta, Story, Canvas } from '@storybook/blocks'
import detailSimpleLanguage from '../../../pages/detailSimpleLanguage.vue'
import * as DetailSingLanguageStories from './DetailSingLanguage.stories';

<Meta of={DetailSingLanguageStories} />

export const Template = (args, { argTypes, viewMode }) => {
  if (viewMode === 'docs') {
    return {
      props: Object.keys(argTypes),
      components: { detailSimpleLanguage },
      template: '<detailSimpleLanguage :isSignLanguage="true" />',
    }
  } else {
    return {
      props: Object.keys(argTypes),
      components: { detailSimpleLanguage },
      template:
        '<detailSimpleLanguage :isSignLanguage="true" :useStickyNavigation="useStickyNavigation" />',
    }
  }
}

# Detailpage sign language example

Please use full width view in order to see the sticky navigation working correctly.

<a href="?path=/story/pages-sing-language-detail-easy-language--example">
  Go to the Canvas Tab
</a>

***

<a href="?id=pages-easy-sign-language-detail-sign-language--example" target="_blank">
  Open in full width in a new tab
</a>

<Canvas>
  <Story of={DetailSingLanguageStories.Example} />
</Canvas>
