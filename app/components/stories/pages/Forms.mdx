import { Meta, Story, Canvas, Controls } from '@storybook/blocks';
import store from '@/.storybook/store';
import FormExample from '../../../pages/formExample';
import * as FormsStories from './Forms.stories';

<Meta of={FormsStories} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { FormExample },
  store: store,
  template: '<FormExample />',
});

# Homepage example

<a href="?path=/story/pages-form-example--example">Go to the Canvas Tab</a>

***

<a href="?id=pages-form-example--form-example&args=&viewMode=story" target="_blank">Open page in full width in a new tab</a>

<Canvas>
  <Story of={FormsStories.FormExample} />
</Canvas>
