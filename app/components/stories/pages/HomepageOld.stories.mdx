
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import store from '@/.storybook/store';
import Index from '../../../pages/homePageOld.vue';

<Meta
  title="Pages/Homepage old"
  component={Index}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Index },
  store: store,
  template: '<Index />',
});

# Homepage example

<a href="?path=/story/pages-homepage-old--example">Go to the Canvas Tab</a>

---

<a href="?id=pages-homepage-old--example" target="_blank">Open in full width in a new tab</a>

<Canvas>
  <Story
    name="Example"
  >
    {Template.bind({})}
  </Story>
</Canvas>

