import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import hubPage from '../../../pages/hubPage.vue'
import * as HubpageStories from './Hubpage.stories'

<Meta of={HubpageStories} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { hubPage },
  template: '<hubPage />',
})

# Hub page example

<a href="?path=/story/pages-hub-page--example">{'Go to the Canvas Tab'}</a>

---

<a href="?id=pages-hub-page--example" target="_blank">
  {'Open page in full width in a new tab'}
</a>

<Canvas>
  <Story of={HubpageStories.Example} />
</Canvas>
