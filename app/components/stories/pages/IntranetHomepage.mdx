import { Meta, Story, Canvas } from '@storybook/blocks'
import Index from '../../../pages/index.vue'
import * as IntranetHomepageStories from './IntranetHomepage.stories'

<Meta of={IntranetHomepageStories} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Index },
  template: '<Index :isIntranet="true" />',
})

# Intranet Homepage example

<a href="?path=/story/pages-intranet-homepage--example">
  {'Go to the Canvas Tab'}
</a>

---

<a href="?id=pages-intranet-homepage--example" target="_blank">
  {'Open in full width in a new tab'}
</a>

Add a `body--intranet` class to the body tag to display the intranet version of the homepage.

<Canvas>
  <Story of={IntranetHomepageStories.Example} />
</Canvas>
