
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import store from '@/.storybook/store';
import Index from '../../../pages/index.vue';

<Meta
  title="Pages/Intranet/Homepage"
  component={Index}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Index },
  store: store,
  template: '<Index :isIntranet="true" />',
});

# Intranet Homepage example

<a href="?path=/story/pages-intranet-homepage--example">Go to the Canvas Tab</a>

---

<a href="?id=pages-intranet-homepage--example" target="_blank">Open in full width in a new tab</a>

Add a `body--intranet` class to the body tag to display the intranet version of the homepage.

<Canvas>
  <Story
    name="Example"
  >
    {Template.bind({})}
  </Story>
</Canvas>

