
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import store from '@/.storybook/store';
import SearchResultsFilters from '../../../pages/SearchResultsFilters.vue';

<Meta
  title="Pages/Search Results with Filters"
  component={SearchResultsFilters}
  argTypes={{
    noResults: { name:'No results', control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { SearchResultsFilters },
  store: store,
  template: '<SearchResultsFilters :noResults="noResults" :isLoading="isLoading" />',
});

# Search results, with filters

<a href="?path=/story/pages-search-results-with-filters--with-results">Go to the Canvas Tab</a>

---

<a href="?id=pages-search-results-with-filters--with-results&args=&viewMode=story" target="_blank">Open page in full width in a new tab</a>

<Canvas>
  <Story
    name="With results"
    args={{
      noResults: false,
      isLoading: false
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# No results

<a href="?path=/story/pages-search-results-advanced--with-results">Go to the Canvas Tab</a>

---

<a href="?id=pages-search-results-advanced--without-results&args=&viewMode=story" target="_blank">Open page in full width in a new tab</a>


<Canvas>
  <Story
    name="Without results"
    args={{
      noResults: true,
      isLoading: false
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>




# Loading example

<a href="?path=/story/pages-search-results-advanced--when-loading">Go to the Canvas Tab</a>

---

<a href="?id=pages-search-results-advanced--when-loading&args=&viewMode=story" target="_blank">Open page in full width in a new tab</a>

<Canvas>
  <Story
    name="When Loading"
    args={{
      noResults: true,
      isLoading: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>


