
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import store from '@/.storybook/store';
import searchResults from '../../../pages/searchResults.vue';

<Meta
  title="Pages/Search Results"
  component={searchResults}
  argTypes={{
    isOpen: { name:'Is open', control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { searchResults },
  store: store,
  template: '<searchResults />',
});

# Search results

<Canvas>
  <Story
    name="Example"
    args={{
      isOpen: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<a href="?path=/story/pages-search-results--example">Go to the Canvas Tab</a>

---

<a href="?id=pages-search-results--example" target="_blank">Open page in full width in a new tab</a>

