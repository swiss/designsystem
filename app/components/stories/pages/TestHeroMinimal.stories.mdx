
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import store from '@/.storybook/store';
import testHeroMinimal from '../../../pages/testHeroMinimal.vue';

<Meta
  title="Pages/Test Hero Minimal"
  component={testHeroMinimal}
  argTypes={{
    isOpen: { name:'Is open', control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { testHeroMinimal },
  store: store,
  template: '<testHeroMinimal />',
});

# Simple page example

A simple detail page with real content to proof and finalize the spacing system

<Canvas>
  <Story
    name="Example"
    args={{
      isOpen: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
