import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import testHeroTitleOnly from '../../../pages/testHeroTitleOnly.vue'
import * as TestHeroTitleOnlyStories from './TestHeroTitleOnly.stories'

<Meta of={TestHeroTitleOnlyStories} />

# Test page for hero with only a title

A detail page with a hero containing only a title.

<Canvas of={TestHeroTitleOnlyStories.Example} />
