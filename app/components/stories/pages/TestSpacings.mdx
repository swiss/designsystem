import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import testSpacings from '../../../pages/testSpacings.vue'
import * as TestSpacingsStories from './TestSpacings.stories'

<Meta of={TestSpacingsStories} />

# Test page for vertical spacings

A simple detail page with real content to proof and finalize the spacing system.

<Canvas of={TestSpacingsStories.Example} />
