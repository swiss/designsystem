
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import store from '@/.storybook/store';
import testSpacings from '../../../pages/testSpacings.vue';

<Meta
  title="Pages/Test Spacings"
  component={testSpacings}
  argTypes={{
    isOpen: { name:'Is open', control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { testSpacings },
  store: store,
  template: '<testSpacings />',
});

# Simple page example

A detail page with a minimal hero, containing only a title.

<Canvas>
  <Story
    name="Example"
    args={{
      isOpen: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
