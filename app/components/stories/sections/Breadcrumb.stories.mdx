import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Breadcrumb from '../../ch/sections/Breadcrumb.vue';

<Meta
  title="Sections/Header/Breadcrumb"
  component={Breadcrumb}
  argTypes={{
    isOpen: { name:'Is open', control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Breadcrumb },
  template: '<Breadcrumb :isOpen="isOpen" />',
});

# Top Bar

<Canvas>
  <Story
    name="Example"
    args={{
      isOpen: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example" />

<a href="?path=/story/sections-header-breadcrumb--example">Go to the Canvas Tab</a>

---

<a href="?id=sections-header-breadcrumb--example" target="_blank">Open page in full width in a new tab</a>



