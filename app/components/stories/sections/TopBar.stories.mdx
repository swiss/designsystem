import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import TopBar from '../../ch/sections/TopBar.vue';

<Meta
  title="Sections/Header/TopBar"
  component={TopBar}
  argTypes={{
    isOpen: { name:'Is open', control: { type: 'boolean' } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { TopBar },
  template: '<TopBar :isOpen="isOpen" />',
});

# Top Bar

<Canvas>
  <Story
    name="Example"
    args={{
      isOpen: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example" />

<a href="?path=/story/sections-header-topbar--example">Go to the Canvas Tab</a>

---

<a href="?id=sections-header-topbar--example" target="_blank">Open page in full width in a new tab</a>



